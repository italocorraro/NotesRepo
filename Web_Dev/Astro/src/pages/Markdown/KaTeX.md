---
layout: ../../layouts/serious/LayoutAstro.astro
title: 'LaTex Math Symbols'
metaTitle: 'LaTex Math Symbols'
description: 'Chat Sheet per simboli matematici in LaTex'
author: 'Italo Corraro'
order: 3
---


## Simboli di base e operatori

| Simbolo | Codice |
|---|---|
| $+, -, \times, \div$ | `+`, `-`, `\times`, `\div` |
| $\cdot$ | `\cdot` |
| $\pm, \mp$ | `\pm`, `\mp` |
| $=, \neq$ | `=`, `\neq` |
| $<, >, \leq, \geq$ | `<`, `>`, `\leq`, `\geq` |
| $\approx, \sim, \equiv$ | `\approx`, `\sim`, `\equiv` |
| $\propto$ | `\propto` |

## Frazioni, radici, potenze

| Simbolo | Codice |
|---|---|
| $\frac{a}{b}$ | `\frac{a}{b}` |
| $\sqrt{x}$, $\sqrt[n]{x}$ | `\sqrt{x}` , `\sqrt[n]{x}` |
| $x^n$, $x_n$ | `x^n` , `x_n` |

## Insiemi e logica

| Simbolo | Codice |
|---|---|
| $\in, \notin$ | `\in`, `\notin` |
| $\subset, \subseteq$ | `\subset`, `\subseteq` |
| $\cup, \cap$ | `\cup`, `\cap` |
| $\emptyset$ | `\emptyset` |
| $\mathbb{N}, \mathbb{Z}, \mathbb{Q}, \mathbb{R}, \mathbb{C}$ | `\mathbb{N}` ecc. |
| $\forall, \exists, \nexists$ | `\forall`, `\exists`, `\nexists` |
| $\Rightarrow, \Leftrightarrow$ | `\Rightarrow`, `\Leftrightarrow` |
| $\neg$ | `\neg` |
| $\setminus$ | `\setminus` |

## Limiti, successioni, infinito

| Simbolo | Codice |
|---|---|
| $\lim_{x \to x_0} f(x)$ | `\lim_{x \to x_0} f(x)` |
| $\infty$ | `\infty` |
| $\to, \nearrow, \searrow$ | `\to`, `\nearrow`, `\searrow` |
| $\sup, \inf$ | `\sup`, `\inf` |
| $\max, \min$ | `\max`, `\min` |
| $\limsup, \liminf$ | `\limsup`, `\liminf` |
| $o(\cdot), O(\cdot)$ (o piccolo, O grande) | `o(\cdot)` , `O(\cdot)` |

## Derivate

| Simbolo | Codice |
|---|---|
| $f'(x)$, $f''(x)$, $f^{(n)}(x)$ | `f'(x)` , `f''(x)` , `f^{(n)}(x)` |
| $\dot{x}$, $\ddot{x}$, $\dddot{x}$ | `\dot{x}` , `\ddot{x}` , `\dddot{x}` |
| $\frac{df}{dx}$, $\frac{d^2f}{dx^2}$ | `\frac{df}{dx}` , `\frac{d^2f}{dx^2}` |
| $\frac{\partial f}{\partial x}$ | `\frac{\partial f}{\partial x}` |
| $\nabla f$ (gradiente) | `\nabla f` |
| $\Delta f$ (Laplaciano/incremento) | `\Delta f` |

## Integrali

| Simbolo | Codice |
|---|---|
| $\int f(x)\,dx$ | `\int f(x)\,dx` |
| $\int_a^b f(x)\,dx$ | `\int_a^b f(x)\,dx` |
| $\oint$ (integrale di linea chiusa) | `\oint` |
| $\iint, \iiint$ (integrali doppi/tripli) | `\iint`, `\iiint` |

## Serie e Sommatorie

| Simbolo | Codice |
|---|---|
| $$\sum_{n=0}^{\infty} a_n$$ | `\sum_{n=0}^{\infty} a_n` |
| $$\prod_{i=1}^{n} a_i$$ | `\prod_{i=1}^{n} a_i` |
| $$\bigcup_{i=1}^{n} a_i$$ | `\bigcup_{i=1}^{n} a_i` |

## Funzioni comuni

| Simbolo | Codice |
|---|---|
| $\sin, \cos, \tan$ | `\sin`, `\cos`, `\tan` |
| $\arcsin, \arccos, \arctan$ | `\arcsin`, `\arccos`, `\arctan` |
| $\sinh, \cosh, \tanh$ | `\sinh`, `\cosh`, `\tanh` |
| $\ln, \log, \exp$ | `\ln`, `\log`, `\exp` |

## Vettori e algebra lineare

| Simbolo | Codice |
|---|---|
| $\vec{v}$ oppure $\mathbf{v}$ | `\vec{v}` , `\mathbf{v}` |
| $\|v\|$ | `\|v\|` |
| $\hat{i}, \hat{j}, \hat{k}$ | `\hat{i}` ecc. |
| $\times$ (prodotto vettoriale) | `\times` |
| $\cdot$ (prodotto scalare) | `\cdot` |
| $\det$ | `\det` |

## Lettere greche

| Simbolo | Codice |
|---|---|
| $\alpha, \beta, \gamma, \delta$ | `\alpha`, `\beta`, `\gamma`, `\delta` |
| $\varepsilon, \epsilon$ | `\varepsilon` , `\epsilon` |
| $\theta, \lambda, \mu, \pi$ | `\theta`, `\lambda`, `\mu`, `\pi` |
| $\sigma, \tau, \phi, \varphi$ | `\sigma`, `\tau`, `\phi`, `\varphi` |
| $\Delta, \Gamma, \Sigma, \Omega$ | `\Delta`, `\Gamma`, `\Sigma`, `\Omega` |

## Frecce e notazioni varie

| Simbolo | Codice |
|---|---|
| $\to, \mapsto$ | `\to`, `\mapsto` |
| $\uparrow, \downarrow$ | `\uparrow`, `\downarrow` |
| $\ldots, \cdots, \vdots, \ddots$ | `\ldots`, `\cdots`, `\vdots`, `\ddots` |
| $\overline{x}, \underline{x}$ | `\overline{x}` , `\underline{x}` |
| $\hat{x}, \tilde{x}$ | `\hat{x}` , `\tilde{x}` |

## Parentesi con dimensione automatica

| Simbolo | Codice |
|---|---|
| $\left( \frac{a}{b} \right)$ | `\left( ... \right)` |
| $\left[ ... \right]$ | `\left[ ... \right]` |
| $\left\{ ... \right\}$ | `\left\{ ... \right\}` |
| $\left\lvert ... \right\rvert$ | `\left\lvert ... \right\rvert` |

## Probabilità e Statistica

| Simbolo | Codice |
|---|---|
| $\binom{n}{k}$ | `\binom{n}{k}` |
| $\dbinom{n}{k}$ | `\dbinom{n}{k}` |